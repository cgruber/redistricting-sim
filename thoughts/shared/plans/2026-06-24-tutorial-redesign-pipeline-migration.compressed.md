<!--COMPRESSED v1; source:2026-06-24-tutorial-redesign-pipeline-migration.md-->
§META
date:2026-06-24 author:Claude+cgruber status:draft
ticket:GAME-096,GAME-097,GAME-076,GAME-077,DESIGN-012

§GOAL
Tutorials generated by pipeline(spec.yaml→JSON)+pedagogical edits like scenarios; pedagogy teaches GAME mechanics NOT electoral(operate tool,read map,satisfy rules)→on-ramp to electoral scenarios. Interactive via guided-overlay engine(highlight UI,sequence steps,unlock views).

§CONTEXT
3 tutorials, all old gen-tutorial-*.main.kts(no specs):
  t001 Millbrook 30prec(rect) 2dist noterrain | t002 Three-District 196prec 3dist | t003 Hawthorn 119prec 4dist terrain+rivers(map-features catch-all)
NO overlay engine exists(only intro slides+1 objective line); DESIGN-012/GAME-076/077 spec it but unbuilt+STALE(ref removed header btns #btn-view-toggle/#btn-county-borders)
View toolbar=district/lean/county; NO city view yet
"unlock" today=scenario-progression lock only(main.ts~212 isCompleted); NO per-tutorial view gating
map shape pipeline-native: map.shape:hex_circle+radius; hex circle=3·r·(r+1)+1 prec (r2=19 r3=37 r4=61 r5=91)

§APPROACH [decisions locked w/ user]
- BUILD guided-overlay engine(not narrative-only); reuse DESIGN-012 step model, reconcile to current toolbar
- BUILD basic city-limits overlay first(cosmetic, reuse county flood-fill/border) → T2 teaches all 3 views
- Pedagogy(game mechanics):
  T1 "Welcome"=paint+submit, smallest hex-circle, select/paint/undo-redo/submit, forgiving
  T2 "Map & Views"=hex_circle r4→61prec,3dist; cosmetic river+coast; partisan lean(read-only,no seat_count); 2-3 counties+1 urban core; View toolbar lean/county/city REVEAL-within-tutorial-only(real scenarios keep all views always-avail,no cross-scenario gating); objective mechanical(3 balanced contiguous) [locked]
  T3 "Reading Rules"=pop-balance bars+contiguity warnings(legal? how to fix) [refine in T2 build]
  T4 "Capstone"=bigger,more dist,all tools,draw-legal-map → bridge to electoral [refine in T2 build]
T1 spec LOCKED: hex_circle r=3→37prec(replaces 30 rect); terrain:{}; 2 dist; neutral 50/50 demo hidden via hide_election_results; NO untaught failure modes→gates on district_count ONLY(balance+contiguity NOT enforced: no balance criterion, contiguity:allowed; taught later); validity panel applicable-aware→hidden in T1 along w/ election prediction; paint-only chrome: hide_view_toolbar(hides view toolbar+forces painter open); legend removed GAME-WIDE(paint toolbar=legend)

§STEPS [staged; 1 PR/ticket]
1. GAME-096 city-limits overlay: cosmetic, reuse county border mech; 3rd Overlays toolbar entry; no gameplay effect; tests render+persist
2. GAME-097 tutorial pipeline migration+T1: author tutorial-001.spec.yaml(locked T1), regen JSON, retire gen-tutorial-*.main.kts; establish tutorial-spec conventions(flag,no-demo path); T2-T4 specs follow
3. DESIGN-012 REVISE: step model+highlight selectors→current toolbar(aria-checked radios/aria-pressed toggles); define view-unlock gating; sign off before engine
4. GAME-076 REVISE: build engine(seq,highlight/dim,input-pause,skip/persist,?resetTutorial=1)+revised T1 script vs 37-prec map+toolbar
5. GAME-077 T2 guided: author tutorial-002.spec.yaml(bigger+terrain)+T2 step script(lean/county/city highlight+unlock), reuse engine
6. T3/T4: new tickets when pedagogy finalized(rules-reading+capstone); author specs+scripts
rationale: city overlay(1)+pipeline conventions(2) prereq for T2 engine; T1(4) proves engine on simplest map before T2(5) exercises views+unlock

§RISKS
stale DESIGN-012 scripts(old btns)→revise selectors before engine code(3 gates 4)
tutorials may not need demographics but sim/adapter expects population→validate no-partisan pipeline path(GAME-097)
view-unlock=new concept,no machinery→design in DESIGN-012 before engine; CSS/state-only,non-destructive like highlight
old gen-tutorial retirement must not orphan build target→check refs before delete(GAME-097)

§DONE
- [ ] all 4 tutorials from tutorial-00N.spec.yaml+edits; old generators retired
- [ ] city-limits overlay shipped, 3rd View toolbar entry
- [ ] overlay engine shipped; T1+T2 scripted interactive(highlight/pause/skip+view-unlock)
- [ ] T1-T4 each distinct game-mechanics lesson(paint/submit;map+views;rules;capstone); none electoral
- [ ] tests: per-ticket unit+e2e

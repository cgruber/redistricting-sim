<!--COMPRESSED v1; source:2026-06-24-tutorial-redesign-pipeline-migration.md-->
§META
date:2026-06-24 author:Claude+cgruber status:draft
ticket:GAME-097(done),DESIGN-012,GAME-076,GAME-077,GAME-096,GAME-098,GAME-099

§GOAL
Tutorials generated by pipeline(spec.yaml→JSON)+pedagogical edits like scenarios; pedagogy=ONE new concept per tutorial(T1 operate tool→T2 legal map→T3 read the vote[electoral]→T4 capstone); electoral layer unveiled deliberately in T3→on-ramp to electoral scenarios. Interactive via guided-overlay engine(highlight UI,sequence steps,reveal controls).

§CONTEXT
3 tutorials, all old gen-tutorial-*.main.kts(no specs):
  t001 Millbrook 30prec(rect) 2dist noterrain | t002 Three-District 196prec 3dist | t003 Hawthorn 119prec 4dist terrain+rivers(map-features catch-all)
NO overlay engine exists(only intro slides+1 objective line); DESIGN-012/GAME-076/077 spec it but unbuilt+STALE(ref removed header btns #btn-view-toggle/#btn-county-borders)
View toolbar=district/lean/county; NO city view yet
"unlock" today=scenario-progression lock only(main.ts~212 isCompleted); NO per-tutorial view gating
map shape pipeline-native: map.shape:hex_circle+radius; hex circle=3·r·(r+1)+1 prec (r2=19 r3=37 r4=61 r5=91)

§APPROACH [decisions locked w/ user]
- BUILD guided-overlay engine(not narrative-only); reuse DESIGN-012 step model, reconcile to current toolbar
- BUILD basic city-limits overlay first(cosmetic, reuse county flood-fill/border) → T2 teaches all 3 views
- Pedagogy REVISED 2026-06-24 — ONE new concept per tutorial; electoral layer unveiled deliberately:
  T1 "Core Loop"=paint+submit, smallest hex-circle, pre-electoral, gates district_count only [SHIPPED GAME-097]
  T2 "A Legal Map"=contiguity+balanced pop; introduce VALIDITY PANEL(balance bars+contiguity); gates district_count+population_balance+contiguity; STILL pre-electoral(hide_election_results+hide_view_toolbar; validity panel shows b/c balance/contig enforced) [locked]
  T3 "Reading the Vote"=FIRST electoral layer; unveil election-result panel TOGETHER w/ lean view(lean→who leans, result→who wins; belong together)+county/city views+terrain; hide_election_results:false,hide_view_toolbar:false; overlay REVEALs lean+result+county+city in sequence [locked]
  T4 "Capstone"=bigger,all tools→bridge to electoral scenarios [refine when reached]
T1 spec LOCKED: hex_circle r=3→37prec(replaces 30 rect); terrain:{}; 2 dist; neutral 50/50 demo hidden via hide_election_results; NO untaught failure modes→gates on district_count ONLY(balance+contiguity NOT enforced: no balance criterion, contiguity:allowed; taught later); validity panel applicable-aware→hidden in T1 along w/ election prediction; paint-only chrome: hide_view_toolbar(hides view toolbar+forces painter open); legend removed GAME-WIDE(paint toolbar=legend)

§STEPS [staged; 1 PR/ticket]
1. GAME-097 pipeline migration+T1 [DONE PR#273]: T1 on pipeline+paint-only chrome flags+conventions
2. DESIGN-012 REVISED: step model,guided:true activation,highlight/pause/skip,reveal-target action,T1-T4 scripts [sign off]
3. GAME-076 engine+T1 paint-only script: build engine(seq,highlight/dim,input-pause,skip/persist,?resetTutorial=1,reveal,guided flag)+T1 5-step; independent of T2-T4 reshuffle
4. GAME-077 RE-SCOPED tutorial-002 "A Legal Map": contiguity+balanced pop+validity panel; hide_election_results+hide_view_toolbar(pre-electoral)+guided script; reuse engine
5. GAME-096 city-limits overlay: cosmetic,reuse county border,3rd Overlays entry; NOW a T3 prereq
6. GAME-098 NEW tutorial-003 "Reading the Vote": terrain+lean+counties+urban core; guided script REVEALs election-result panel WITH lean view, then county+city; first electoral; needs GAME-096
7. GAME-099 NEW tutorial-004 "Capstone": full map,all tools→bridge to scenarios
rationale: engine(3)+T1 prove overlay simplest; T2(4) pre-electoral,no new infra; city overlay(5) gates T3(6)=first to exercise reveal+electoral panels

§RISKS
stale DESIGN-012 scripts(old btns)→revise selectors before engine code(3 gates 4)
tutorials may not need demographics but sim/adapter expects population→validate no-partisan pipeline path(GAME-097)
view-unlock=new concept,no machinery→design in DESIGN-012 before engine; CSS/state-only,non-destructive like highlight
old gen-tutorial retirement must not orphan build target→check refs before delete(GAME-097)

§DONE
- [ ] all 4 tutorials from tutorial-00N.spec.yaml+edits; old generators retired
- [ ] city-limits overlay shipped, 3rd View toolbar entry
- [ ] overlay engine shipped; tutorials scripted interactive(highlight/pause/skip+reveal)
- [ ] T1-T4 each ONE concept(T1 paint/submit;T2 legal map=contiguity+balance;T3 read the vote=lean+result+views;T4 capstone); electoral unveiled in T3
- [ ] tests: per-ticket unit+e2e

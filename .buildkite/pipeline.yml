# Buildkite CI Pipeline
#
# Stack is being decided — see SPIKE-001 (game tech stack) and SPIKE-002 (build system).
# Steps will be added once the spike results are merged and the stack is locked.

env:
  BUILDKITE_CLEAN_CHECKOUT: "true"

steps:
  - label: ":memo: No build steps yet"
    command: echo "Stack spike in progress — see SPIKE-001 and SPIKE-002 tickets"
